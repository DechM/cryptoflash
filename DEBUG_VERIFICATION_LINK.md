# 🔍 Debug Verification Link - Стъпки за диагностика

## Проблем: Линкът от email-а показва "Invalid Link"

Console не показва логове от `/auth/verify` route-а, което означава че линкът не стига до нашия endpoint.

---

## 🔍 СТЪПКА 1: Копирай точния URL от email-а

1. Отвори email-а в Gmail
2. **НЕ кликвай на бутона/линка още!**
3. **Кликни с десен бутон** на линка → "Copy link address"
4. Сподели точния URL тук

**Пример как трябва да изглежда:**
```
https://knaqsjurhktuoonemxcm.supabase.co/auth/v1/verify?token=...&type=signup&redirect_to=https://cryptoflash.app/auth/verify
```

---

## 🔍 СТЪПКА 2: Провери Network Tab

1. Отвори Developer Tools (F12)
2. Отиди на вкладката **Network**
3. Кликни на verification link-а от email-а
4. Виж дали има request към:
   - `/auth/verify` (нашия endpoint)
   - Или към `supabase.co/auth/v1/verify` (Supabase endpoint)

**Очаквано поведение:**
- Първо: request към `supabase.co/auth/v1/verify` (Supabase обработва токена)
- След това: redirect към `cryptoflash.app/auth/verify` (нашия endpoint)

---

## 🔍 СТЪПКА 3: Провери Console при кликване

1. Очисти Console (trash icon)
2. Кликни на verification link-а
3. Виж какво се логва

**Очаквани логове (от нашия код):**
- `User already authenticated via session: ...`
- `Verifying token directly...`
- `Verification failed: no token/code and no session` (с детайли)

**Ако няма логове:** Линкът не стига до нашия route.

---

## 🤔 Възможни причини:

### 1. Supabase не пренасочва правилно
**Причина:** `emailRedirectTo` URL не е точно в Redirect URLs списъка
**Решение:** Провери в Supabase → Authentication → URL Configuration

### 2. Линкът е на различен формат
**Причина:** Supabase изпраща линк към `supabase.co`, а не директно към нашия сайт
**Решение:** Supabase трябва да пренасочи автоматично, но ако не работи - проверка на Redirect URLs

### 3. Токенът е в hash fragment
**Причина:** Някои браузъри/email клиенти поставят токена в `#` вместо query params
**Решение:** Вече добавих обработка за това в кода

---

## 📋 КАЖИ МИ:

1. **Точния URL от email-а** (копиран преди кликване)
2. **Какво се вижда в Network tab** когато кликнеш на линка
3. **Какво се логва в Console** когато кликнеш на линка

С тези данни ще знам точно какъв е проблемът!

